<Project Sdk="Microsoft.NET.Sdk.Razor">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <RazorLangVersion>3.0</RazorLangVersion>
    <Description>Provides core functionalities for handling AJAX form submissions with zero or more files, and report back data upload progress.</Description>
    <Copyright>© 2020 Karfamsoft. All rights reserved.</Copyright>
    <Authors>Abdourahamane Kaba</Authors>
    <Company>Karfamsoft</Company>
    <PackageLicenseExpression>Apache-2.0+</PackageLicenseExpression>
    <NeutralLanguage>en-US</NeutralLanguage>
    <PackageRequireLicenseAcceptance>true</PackageRequireLicenseAcceptance>
    <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
    <RepositoryUrl>https://github.com/bigabdoul/BlazorFormManager</RepositoryUrl>
    <RepositoryType>Git</RepositoryType>
    <PackageTags>c#, .net, razor, class, library, blazor, form, form-manager, forms, javascript, interop, xmlhttprequest, client, server</PackageTags>
    <PackageProjectUrl>https://github.com/bigabdoul/BlazorFormManager</PackageProjectUrl>
    <PackageReleaseNotes>Version 2.1 introduces breaking changes due to huge code refactoring. New types and custom attributes have been added. They make automatic form generation easier than ever before thanks to InputFileAttribute and ImagePreviewAttribute.  ImagePreviewAttribute ensures that no marshalling occurs between JS and .NET when generating an image preview which improves performance and memory usage on the client device.

Advanced support for JavaScript's FileReader API (readAsArrayBuffer, readAsBinaryString, readAsDataURL, readAsText) makes it possible to read files from a user's device and generate image and/or text previews without writing a single line of JavaScript code.

New helper components for file selection, displaying simple error messages, and generating image previews have been added.

Accept only specific file extensions and/or types.
So much more...</PackageReleaseNotes>
    <Version>2.1.0</Version>
  </PropertyGroup>

  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Debug|AnyCPU'">
    <DocumentationFile>C:\Users\akaba\source\repos\BlazorFormManager\src\BlazorFormManager\BlazorFormManager.xml</DocumentationFile>
    <DefineConstants>TRACE</DefineConstants>
  </PropertyGroup>


  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.Components" Version="3.1.6" />
    <PackageReference Include="Microsoft.AspNetCore.Components.Web" Version="3.1.6" />
  </ItemGroup>

</Project>
