@*<QuillToolbar Id="@ToolbarId"/>*@
@if (Target.IsBlank() && OnChange.HasDelegate == false)
{
    <div id="@EditorId" class="QuillEditor quill-editor rich-text-editor" />
    <input type="hidden" id="@(EditorId)_hidden" @attributes="@(GetAttributes(("name", Name)))" />
}